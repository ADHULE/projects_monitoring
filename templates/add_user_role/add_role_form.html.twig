<div class="container" style=" position: relative; background-color: rgb(240, 255, 255); margin-top:5%;">
  {{ form_start(form) }}
  {{ form_widget(form) }}
  <div class="row">
    <div class="col-md-6 col-lg-3">
      <button class="btn btn-info">{{ button_label|default('Update') }}</button>
    </div>
    <div class="col-md-6 col-lg-3">
      <a
        class="btn btn-outline-warning"
        href="{{ path('app_developpeur_index') }}"
        >back to list</a
      >
    </div>
    <div class="col-md-6 col-lg-3">
      {{ include("developpeur/_delete_form.html.twig") }}
    </div>
  </div>
  {{ form_end(form) }}
</div>
