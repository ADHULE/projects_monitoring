{% extends 'base.html.twig' %} {% block title %}Developpeur {% endblock %} {%
block body %}
<h1>Developper</h1>

<div class="container show">
  <div class="row">
        <div class="col">
          <p>
            <h3> Name</h3>
            {{ user.name}}
          </p>
          <p>
            <h3> Email</h3>
            {{ user.email }}
          </p>
        
          <p>
           <h3> Skills</h3>
           {{ user.lastname }}
          </p>
          <p>
           <h3> Roles</h3>
              {{ user.roles ? user.roles|json_encode : '' }}
           
          </p>
          
      </div>
      <div class="col">
        <img
          style="width: 400px"
          src="{{ asset('uploads/images/'~ user.photo) }}"
          alt="..."
        />
      </div>
     
    </div>
    <div class="container text-center">
        <div class="row align-items-center">
            <div class="col"> 
                <a class="btn btn-outline-info" href="{{ path('app_user_index') }}"
                  >back to list</a
                >
            </div>
            <div class="col">
            {# {% if  is_granted("ROLE_USER") %} #}

                  {# <a class="btn btn-outline-warning" href="{{ path('app_developpeur_edit_role', { id: user.id }) }}">Add Role</a> #}

            {# {% else if is_granted("ROLE_DEV") %} #}

                <a class="btn btn-outline-info"href="{{ path('app_user_edit', { id: user.id }) }}">edit</a>
              
            {# {% endif %} #}
            </div>
            <div class="col">

                {{ include("user/_delete_form.html.twig") }}

            </div>
        </div>

    </div>
</div>
{% endblock %}
