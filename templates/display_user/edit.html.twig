{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="text-center mb-4">Edit User</h1>
    <div class="card">
        <div class="card-body">
            {{ include('display_user/_form.html.twig', {'button_label': 'Update'}) }}
            <a href="{{ path('app_user_profile', {'id': user.id}) }}" class="btn btn-secondary mt-3">Back to profile</a>
          {# {{ include('display_user/_delete_form.html.twig') }}     #}
        </div>
    </div>
</div>
{% endblock %}
