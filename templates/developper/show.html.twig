{% extends 'base.html.twig' %}

{% block title %}Developper{% endblock %}

{% block body %}
    <h1>Developper</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ developper.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ developper.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ developper.roles ? developper.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ developper.password }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ developper.name }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ developper.lastname }}</td>
            </tr>
            <tr>
                <th>Photo</th>
                <td>{{ developper.photo }}</td>
            </tr>
            <tr>
                <th>Hire_date</th>
                <td>{{ developper.hireDate ? developper.hireDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Skills</th>
                <td>{{ developper.skills }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_developper_index') }}">back to list</a>

    <a href="{{ path('app_developper_edit', {'id': developper.id}) }}">edit</a>

    {{ include('developper/_delete_form.html.twig') }}
{% endblock %}
